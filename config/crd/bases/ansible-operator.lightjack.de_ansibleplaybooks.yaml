---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ansibleplaybooks.ansible-operator.lightjack.de
spec:
  group: ansible-operator.lightjack.de
  names:
    kind: AnsiblePlaybook
    listKind: AnsiblePlaybookList
    plural: ansibleplaybooks
    singular: ansibleplaybook
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AnsiblePlaybook is the Schema for the ansibleplaybooks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AnsiblePlaybook
            properties:
              git:
                description: AnsiblePlaybookGit specifies the Git repository containing
                  the Ansible playbook and requirements file. Mutually exclusive with
                  the Inline field.
                properties:
                  playbookPath:
                    description: PlaybookPath is the path to the Ansible playbook
                      file within the Git repository
                    type: string
                  repo:
                    description: AnsiblePlaybookRepo specifies the Git repository
                      URL and reference (branch, tag, or commit).
                    properties:
                      ref:
                        default: main
                        description: Ref is the Git reference (branch, tag, or commit)
                          to use, default 'main'
                        type: string
                      url:
                        description: URL is the Git repository URL
                        type: string
                    required:
                    - url
                    type: object
                  requirementsPath:
                    description: RequirementsPath is the path to the Ansible requirements.yml
                      file within the Git repository
                    type: string
                required:
                - playbookPath
                - repo
                type: object
              inline:
                description: AnsiblePlaybookInline specifies the Ansible playbook
                  and requirements file as inline strings. Mutually exclusive with
                  the Git field.
                properties:
                  playbook:
                    description: Playbook is the content of the Ansible playbook file
                      as a string
                    type: string
                  requirements:
                    description: Requirements is the content of the Ansible requirements.yml
                      file as a string
                    type: string
                required:
                - playbook
                type: object
            type: object
            x-kubernetes-validations:
            - message: Inline and Git are mutually exclusive
              rule: has(self.inline) != has(self.git)
          status:
            description: status defines the observed state of AnsiblePlaybook
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the AnsiblePlaybook resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
