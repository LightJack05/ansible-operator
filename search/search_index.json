{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ansible-operator","text":"<p>ansible-operator is a Kubernetes operator that runs Ansible playbooks against SSH-reachable hosts, on a schedule, using nothing but Kubernetes resources.</p> <p>Instead of maintaining an inventory file, a control node, and a cron entry somewhere, you describe your hosts, groups, and playbooks as Custom Resources. The operator generates the Ansible inventory, manages SSH host-key trust, and dispatches playbook runs as Kubernetes <code>CronJob</code>s.</p> <pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleReconcileJob\nmetadata:\n  name: nightly-config\n  namespace: default\nspec:\n  schedule: \"0 0 * * *\"        # every night at midnight\n  playbookRef:\n    name: base-config          # an AnsiblePlaybook in the same namespace\n</code></pre>"},{"location":"#why-use-it","title":"Why use it","text":"<ul> <li>Declarative Ansible \u2014 hosts, groups, playbooks, and schedules are all Kubernetes objects   you can <code>kubectl apply</code>, GitOps, and RBAC like anything else in your cluster.</li> <li>Automatic inventory generation \u2014 the operator renders a full Ansible inventory (hosts,   groups, subgroups, host/group vars) from your resources on every change.</li> <li>SSH host-key management \u2014 host keys are scanned and stored on first connect, so runs stay   protected against man-in-the-middle without manual <code>known_hosts</code> juggling.</li> <li>Scheduled reconciliation \u2014 every <code>AnsibleReconcileJob</code> becomes a Kubernetes <code>CronJob</code>, so   playbook runs get the reliability, history, and observability of native workloads.</li> <li>Status you can watch \u2014 each resource reports Kubernetes conditions (<code>Ready</code>, <code>Progressing</code>,   <code>Successful</code>, ...) so you can tell at a glance whether the last run worked.</li> </ul>"},{"location":"#the-resources-at-a-glance","title":"The resources at a glance","text":"Resource Purpose <code>AnsibleHost</code> A single SSH-reachable host and its credentials. <code>AnsibleGroup</code> A named group of hosts and/or subgroups. <code>AnsiblePlaybook</code> A playbook, stored inline or fetched from Git. <code>AnsibleReconcileJob</code> A schedule that runs a playbook against the inventory."},{"location":"#where-to-next","title":"Where to next","text":"<ul> <li> Installation \u2014 install the operator with Helm or plain manifests.</li> <li> Quick Start \u2014 go from zero to a scheduled playbook run.</li> <li> Architecture \u2014 understand how the pieces fit together.</li> <li> Custom Resources \u2014 the full API reference.</li> </ul> <p>Project status</p> <p>ansible-operator is at API version <code>v1alpha1</code>. The API may still change between releases.</p>"},{"location":"concepts/architecture/","title":"Architecture","text":"<p>ansible-operator follows the standard Kubernetes operator pattern: a manager process runs a set of controllers, each watching one Custom Resource type and reconciling the cluster toward the declared state. Actual playbook execution is delegated to Kubernetes <code>CronJob</code>s so that runs get native scheduling, retries, and log history.</p>"},{"location":"concepts/architecture/#controllers","title":"Controllers","text":"<p>The manager runs four controllers, one per resource kind.</p>"},{"location":"concepts/architecture/#ansiblehostreconciler","title":"AnsibleHostReconciler","text":"<p>Prepares a host for use:</p> <ol> <li>Verifies the referenced SSH private-key <code>Secret</code> exists and contains a valid key under <code>ssh_key</code>.</li> <li>If host-key verification is enabled (<code>ssh.ignoreHostKey: false</code>), performs an SSH key scan of the    host and stores the trusted host key in the referenced host-key secret. It only writes the key when    one isn't already present, so the trust is pinned on first connect.</li> <li>Materializes the host's <code>hostVars</code> into a <code>ConfigMap</code>.</li> <li>Reports a <code>Ready</code> condition summarizing the result.</li> </ol>"},{"location":"concepts/architecture/#ansiblegroupreconciler","title":"AnsibleGroupReconciler","text":"<p>Validates group membership and rolls up health:</p> <ol> <li>Materializes <code>groupVars</code> into a <code>ConfigMap</code>.</li> <li>Checks that every referenced host and subgroup exists (<code>ReferencesValid</code>) and is itself healthy    (<code>Healthy</code>).</li> <li>Sets <code>Ready</code> only when references are valid and all members are healthy.</li> </ol> <p>It uses field indexers on host and subgroup references, so a change to any referenced <code>AnsibleHost</code> or <code>AnsibleGroup</code> automatically re-triggers reconciliation of the groups that depend on it.</p>"},{"location":"concepts/architecture/#ansibleplaybookreconciler","title":"AnsiblePlaybookReconciler","text":"<p>Currently a lightweight controller. Playbook specs are validated by the API server's schema (including the CEL rule that <code>inline</code> and <code>git</code> are mutually exclusive). The stored playbook is consumed at run time by the reconcile job.</p>"},{"location":"concepts/architecture/#ansiblereconcilejobreconciler","title":"AnsibleReconcileJobReconciler","text":"<p>The workhorse. On each reconcile it:</p> <ol> <li>Builds the inventory. It lists all <code>AnsibleHost</code> and <code>AnsibleGroup</code> objects in the namespace and    renders an Ansible inventory (<code>hosts.yaml</code>) with per-host <code>ansible_host</code>, <code>ansible_port</code>,    <code>ansible_user</code>, <code>ansible_become</code>, and SSH key path variables, plus group and subgroup structure.</li> <li>Aggregates known hosts. It collects the trusted SSH host keys from every host into a    <code>known_hosts</code> <code>ConfigMap</code>.</li> <li>Stages the playbook. For inline playbooks it stores the playbook and requirements as ConfigMap    data; for Git playbooks it records the repo URL, ref, and paths for the init container to clone.</li> <li>Creates/updates a <code>CronJob</code> named after the reconcile job. The job's pod template has:<ul> <li>an init container that fetches the playbook (cloning from Git when needed) and prepares    Ansible dependencies into shared volumes, and</li> <li>a runner container that executes <code>ansible-playbook</code> with all of the above mounted in.</li> </ul> </li> <li>Reports status from the newest child <code>Job</code> \u2014 <code>Progressing</code>, <code>Successful</code>, and <code>Ready</code>.</li> </ol> <p>If reconciliation fails, the controller suspends the CronJob and sets <code>Ready=False</code> with a message, so a broken configuration can't keep firing runs.</p> <p>Like the group controller, it watches the resources a run depends on: changes to any <code>AnsibleHost</code>, <code>AnsibleGroup</code>, or the referenced <code>AnsiblePlaybook</code> re-trigger reconciliation and regenerate the inventory.</p>"},{"location":"concepts/architecture/#data-flow-into-a-run","title":"Data flow into a run","text":"<p>When a scheduled job fires, the runner pod receives everything it needs as mounted volumes:</p> Mount Source Contents <code>/inventory/hosts.yaml</code> generated ConfigMap The rendered Ansible inventory <code>/inventory/group_vars/&lt;group&gt;</code> group ConfigMaps Per-group <code>group_vars</code> <code>/inventory/host_vars/&lt;host&gt;</code> host ConfigMaps Per-host <code>host_vars</code> <code>/ssh/known_hosts</code> generated ConfigMap Trusted host keys <code>/ssh/keys/&lt;host&gt;</code> per-host Secrets SSH private keys <code>/config/</code> playbook ConfigMap / env Inline playbook or Git repo details"},{"location":"concepts/architecture/#security-model","title":"Security model","text":"<ul> <li>Host-key trust is pinned on first connect and reused, rather than blindly accepting keys on every   run. You can opt out per host with <code>ssh.ignoreHostKey: true</code>.</li> <li>Credentials never leave Secrets. Private keys are referenced by name and mounted into runner pods,   not copied into specs.</li> <li>No SSH passwords. The API deliberately has no password field \u2014 authentication is key-based.</li> <li>The manager runs as a non-root, read-only-root-filesystem container with all Linux capabilities   dropped (see the Helm values).</li> </ul> <p><code>hostVars</code> / <code>groupVars</code> are inserted verbatim</p> <p>These fields are injected directly into the generated Ansible vars. Treat them as trusted input and avoid templating untrusted data into them.</p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#prerequisites","title":"Prerequisites","text":"<ul> <li>A Kubernetes cluster, v1.11.3+ (a recent version is recommended for full CRD/CEL validation support).</li> <li><code>kubectl</code> configured against the cluster.</li> <li>Permissions to install CRDs and cluster/namespaced RBAC (typically <code>cluster-admin</code>).</li> <li>For Helm installs: Helm v3.8+ (OCI registry support).</li> </ul> <p>The operator itself is published to the GitHub Container Registry:</p> <ul> <li>Manager image: <code>ghcr.io/lightjack05/ansible-operator</code></li> <li>Runner images (pulled automatically by scheduled jobs):   <code>ghcr.io/lightjack05/ansible-operator-runner-init</code> and   <code>ghcr.io/lightjack05/ansible-operator-runner-runner</code></li> </ul> <p>Target host requirements</p> <p>The operator runs Ansible inside cluster pods, so your nodes don't need Ansible installed. Each target host only needs to be reachable over SSH and satisfy Ansible's usual requirement of a Python interpreter. See Architecture for details.</p>"},{"location":"getting-started/installation/#option-1-helm-recommended","title":"Option 1: Helm (recommended)","text":"<p>The chart is published as an OCI artifact:</p> <pre><code>helm install ansible-operator \\\n  oci://ghcr.io/lightjack05/charts/ansible-operator \\\n  --namespace ansible-operator-system \\\n  --create-namespace\n</code></pre> <p>To pin a specific chart version, add <code>--version &lt;x.y.z&gt;</code>.</p>"},{"location":"getting-started/installation/#overriding-values","title":"Overriding values","text":"<p>Pass <code>--set</code> flags or a values file to customize the deployment. For example, to run two replicas with leader election:</p> <pre><code>helm install ansible-operator \\\n  oci://ghcr.io/lightjack05/charts/ansible-operator \\\n  --namespace ansible-operator-system --create-namespace \\\n  --set manager.replicas=2\n</code></pre> <p>The most useful values are the manager image and resources; the full list is documented in the Helm Values reference.</p> <p>Pinning the image</p> <p>The chart defaults the manager image to <code>controller:latest</code>. When installing from the published chart you normally want to pin it to the released image: <pre><code>--set manager.image.repository=ghcr.io/lightjack05/ansible-operator \\\n--set manager.image.tag=latest\n</code></pre></p>"},{"location":"getting-started/installation/#upgrading","title":"Upgrading","text":"<pre><code>helm upgrade ansible-operator \\\n  oci://ghcr.io/lightjack05/charts/ansible-operator \\\n  --namespace ansible-operator-system\n</code></pre> <p>By default the chart keeps the CRDs on uninstall (<code>crd.keep=true</code>) so your custom resources survive an accidental <code>helm uninstall</code>.</p>"},{"location":"getting-started/installation/#uninstalling","title":"Uninstalling","text":"<pre><code>helm uninstall ansible-operator --namespace ansible-operator-system\n</code></pre>"},{"location":"getting-started/installation/#option-2-bundled-manifests","title":"Option 2: Bundled manifests","text":"<p>Every release also ships a single, self-contained manifest bundle. Apply it directly with <code>kubectl</code>:</p> <pre><code>kubectl apply -f https://raw.githubusercontent.com/LightJack05/ansible-operator/main/dist/install.yaml\n</code></pre> <p>Replace <code>main</code> with a release tag to install a specific version. This installs the CRDs, RBAC, and the manager <code>Deployment</code> into the <code>ansible-operator-system</code> namespace.</p> <p>To uninstall:</p> <pre><code>kubectl delete -f https://raw.githubusercontent.com/LightJack05/ansible-operator/main/dist/install.yaml\n</code></pre>"},{"location":"getting-started/installation/#option-3-from-source-development","title":"Option 3: From source (development)","text":"<p>Clone the repository and use the provided <code>Makefile</code> targets. This is aimed at contributors and local testing.</p> <pre><code># Install the CRDs into the cluster your kubeconfig points at\nmake install\n\n# Build and push your own manager image\nmake docker-build docker-push IMG=&lt;your-registry&gt;/ansible-operator:dev\n\n# Deploy the manager using that image\nmake deploy IMG=&lt;your-registry&gt;/ansible-operator:dev\n</code></pre> <p>To tear things down again:</p> <pre><code>make undeploy   # remove the manager Deployment\nmake uninstall  # remove the CRDs\n</code></pre> <p>Run <code>make help</code> for the full list of targets.</p>"},{"location":"getting-started/installation/#verifying-the-install","title":"Verifying the install","text":"<p>Check that the manager pod is running:</p> <pre><code>kubectl get pods -n ansible-operator-system\n</code></pre> <p>And that the CRDs are registered:</p> <pre><code>kubectl get crds | grep ansible-operator.lightjack.de\n</code></pre> <p>You should see <code>ansiblehosts</code>, <code>ansiblegroups</code>, <code>ansibleplaybooks</code>, and <code>ansiblereconcilejobs</code>.</p> <p>Next, head to the Quick Start.</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>This walkthrough takes you from a freshly installed operator to a playbook that runs on a schedule. It assumes you've completed the installation and have a host you can reach over SSH.</p> <p>All resources in this guide live in the <code>default</code> namespace. The operator works per-namespace: an <code>AnsibleReconcileJob</code> builds its inventory from the <code>AnsibleHost</code> and <code>AnsibleGroup</code> objects in the same namespace.</p>"},{"location":"getting-started/quickstart/#1-create-the-ssh-credential-secret","title":"1. Create the SSH credential secret","text":"<p>Every host references a Kubernetes <code>Secret</code> holding its SSH private key under the key <code>ssh_key</code>:</p> <pre><code># Private key the operator uses to authenticate to the host\nkubectl create secret generic web1-creds \\\n  --from-file=ssh_key=$HOME/.ssh/id_ed25519\n</code></pre> <p>The host also references a second secret for the trusted host key (<code>sshHostKeySecretRef</code> below) \u2014 you don't need to create that one; the operator creates and populates it on first reconcile.</p> <p>Key must be usable non-interactively</p> <p>The private key must not be passphrase-protected, since Ansible runs unattended inside a pod.</p>"},{"location":"getting-started/quickstart/#2-define-a-host","title":"2. Define a host","text":"web1-host.yaml<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleHost\nmetadata:\n  name: web1\n  namespace: default\nspec:\n  ansibleName: web1\n  connection:\n    host: 10.0.0.11      # hostname or IP the pod can reach\n    port: 22\n    user: root\n  ssh:\n    ignoreHostKey: false\n    sshKeySecretRef:\n      name: web1-creds     # The secret containing the private key\n    sshHostKeySecretRef:\n      name: web1-host-key  # The secret that will contain the host public key\n  privilege:\n    become: false\n</code></pre> <pre><code>kubectl apply -f web1-host.yaml\nkubectl get ansiblehost web1 -o jsonpath='{.status.conditions}'\n</code></pre> <p>Wait until the host reports <code>Ready=True</code>. See AnsibleHost for every field.</p>"},{"location":"getting-started/quickstart/#3-optional-group-your-hosts","title":"3. (Optional) Group your hosts","text":"<p>If you have several hosts, group them so playbooks can target them by name. A group can also contain other groups as subgroups.</p> webservers-group.yaml<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleGroup\nmetadata:\n  name: webservers\n  namespace: default\nspec:\n  ansibleName: webservers\n  hosts:\n    - name: web1\n  groups: []\n</code></pre> <pre><code>kubectl apply -f webservers-group.yaml\n</code></pre>"},{"location":"getting-started/quickstart/#4-define-a-playbook","title":"4. Define a playbook","text":"<p>Playbooks can be inlined directly into the resource or fetched from Git. Here's an inline example that pings every host:</p> ping-playbook.yaml<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsiblePlaybook\nmetadata:\n  name: ping-all\n  namespace: default\nspec:\n  inline:\n    playbook: |\n      - name: Ping all hosts\n        hosts: all\n        tasks:\n          - name: Ping\n            ansible.builtin.ping:\n</code></pre> <pre><code>kubectl apply -f ping-playbook.yaml\n</code></pre> <p>See AnsiblePlaybook for the Git-based variant and <code>requirements.yml</code> handling.</p>"},{"location":"getting-started/quickstart/#5-schedule-the-run","title":"5. Schedule the run","text":"<p>An <code>AnsibleReconcileJob</code> ties a playbook to a cron schedule. The operator generates the inventory from your hosts and groups, then creates a Kubernetes <code>CronJob</code> that executes the playbook.</p> nightly-job.yaml<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleReconcileJob\nmetadata:\n  name: nightly-ping\n  namespace: default\nspec:\n  schedule: \"0 0 * * *\"    # every day at midnight\n  playbookRef:\n    name: ping-all\n</code></pre> <pre><code>kubectl apply -f nightly-job.yaml\n</code></pre>"},{"location":"getting-started/quickstart/#6-watch-it-work","title":"6. Watch it work","text":"<p>The operator creates a <code>CronJob</code> named after the reconcile job:</p> <pre><code>kubectl get cronjob nightly-ping\n</code></pre> <p>To run it immediately instead of waiting for the schedule, trigger a manual job from the CronJob:</p> <pre><code>kubectl create job --from=cronjob/nightly-ping ping-now\nkubectl logs job/ping-now --all-containers --follow\n</code></pre> <p>Check the reconcile job's status conditions to see the outcome of the most recent run:</p> <pre><code>kubectl get ansiblereconcilejob nightly-ping -o jsonpath='{.status.conditions}'\n</code></pre> <ul> <li><code>Progressing=True</code> \u2014 a run is currently executing.</li> <li><code>Successful=True</code> \u2014 the last run finished without failed tasks.</li> <li><code>Ready=True</code> \u2014 the job reconciled correctly and is scheduled.</li> </ul> <p>If something looks wrong, see Troubleshooting.</p>"},{"location":"getting-started/quickstart/#recap","title":"Recap","text":"<p>You created:</p> <ol> <li>Two secrets (private key + host-key store).</li> <li>An <code>AnsibleHost</code> describing where and how to connect.</li> <li>An <code>AnsibleGroup</code> (optional) to organize hosts.</li> <li>An <code>AnsiblePlaybook</code> with the tasks to run.</li> <li>An <code>AnsibleReconcileJob</code> binding the playbook to a schedule.</li> </ol> <p>From here, the operator keeps the inventory in sync whenever you add, change, or remove hosts and groups.</p>"},{"location":"reference/helm-values/","title":"Helm Values","text":"<p>The Helm chart is published as an OCI artifact at <code>oci://ghcr.io/lightjack05/charts/ansible-operator</code>. This page documents its configurable values. See Installation for install commands.</p>"},{"location":"reference/helm-values/#manager","title":"Manager","text":"<p>Settings for the controller-manager <code>Deployment</code>.</p> Value Default Description <code>manager.replicas</code> <code>1</code> Number of manager replicas. Use leader election (default) when running more than one. <code>manager.image.repository</code> <code>controller</code> Manager image repository. Set to <code>ghcr.io/lightjack05/ansible-operator</code> for the released image. <code>manager.image.tag</code> <code>latest</code> Manager image tag. <code>manager.image.pullPolicy</code> <code>IfNotPresent</code> Image pull policy. <code>manager.args</code> <code>[\"--leader-elect\"]</code> Arguments passed to the manager binary. <code>manager.env</code> <code>[]</code> Extra environment variables. <code>manager.envOverrides</code> <code>{}</code> Per-variable overrides (<code>--set manager.envOverrides.VAR=value</code>); takes precedence over <code>env</code>. <code>manager.imagePullSecrets</code> <code>[]</code> Image pull secrets for private registries. <code>manager.resources.limits</code> <code>cpu: 500m</code>, <code>memory: 128Mi</code> Resource limits. <code>manager.resources.requests</code> <code>cpu: 10m</code>, <code>memory: 64Mi</code> Resource requests. <code>manager.podSecurityContext</code> <code>runAsNonRoot: true</code>, <code>seccompProfile.type: RuntimeDefault</code> Pod-level security context. <code>manager.securityContext</code> <code>allowPrivilegeEscalation: false</code>, <code>capabilities.drop: [ALL]</code>, <code>readOnlyRootFilesystem: true</code> Container-level security context. <code>manager.affinity</code> <code>{}</code> Pod affinity rules. <code>manager.nodeSelector</code> <code>{}</code> Node selector. <code>manager.tolerations</code> <code>[]</code> Pod tolerations."},{"location":"reference/helm-values/#crds","title":"CRDs","text":"Value Default Description <code>crd.enable</code> <code>true</code> Install the CRDs together with the chart. <code>crd.keep</code> <code>true</code> Keep the CRDs (and thus your custom resources) when the release is uninstalled."},{"location":"reference/helm-values/#metrics","title":"Metrics","text":"Value Default Description <code>metrics.enable</code> <code>true</code> Expose the RBAC-protected <code>/metrics</code> endpoint. <code>metrics.port</code> <code>8443</code> Port the metrics server listens on."},{"location":"reference/helm-values/#integrations","title":"Integrations","text":"Value Default Description <code>rbacHelpers.enable</code> <code>false</code> Install convenience admin/editor/viewer RBAC roles for the custom resources. <code>certManager.enable</code> <code>false</code> Enable cert-manager integration for TLS certificates. <code>prometheus.enable</code> <code>false</code> Install a Prometheus <code>ServiceMonitor</code> for scraping metrics (requires prometheus-operator)."},{"location":"reference/helm-values/#naming-overrides","title":"Naming overrides","text":"Value Default Description <code>nameOverride</code> (unset) Partially override the generated chart fullname (keeps the release name). <code>fullnameOverride</code> (unset) Fully override the generated chart fullname."},{"location":"reference/helm-values/#example-values-file","title":"Example values file","text":"values.yaml<pre><code>manager:\n  replicas: 2\n  image:\n    repository: ghcr.io/lightjack05/ansible-operator\n    tag: latest\n  resources:\n    limits:\n      cpu: \"1\"\n      memory: 256Mi\n\nmetrics:\n  enable: true\n\nprometheus:\n  enable: true\n</code></pre> <pre><code>helm install ansible-operator \\\n  oci://ghcr.io/lightjack05/charts/ansible-operator \\\n  -n ansible-operator-system --create-namespace \\\n  -f values.yaml\n</code></pre>"},{"location":"reference/troubleshooting/","title":"Troubleshooting","text":"<p>A checklist for the most common issues, working from the operator down to the runner pods.</p>"},{"location":"reference/troubleshooting/#inspecting-state","title":"Inspecting state","text":"<p>Start by reading the status conditions of the resource that isn't behaving:</p> <pre><code>kubectl get ansiblehost &lt;name&gt; -o jsonpath='{.status.conditions}' | jq\nkubectl get ansiblegroup &lt;name&gt; -o jsonpath='{.status.conditions}' | jq\nkubectl get ansiblereconcilejob &lt;name&gt; -o jsonpath='{.status.conditions}' | jq\n</code></pre> <p>The <code>reason</code> and <code>message</code> fields usually point straight at the problem. Also check the manager logs:</p> <pre><code>kubectl logs -n ansible-operator-system deploy/ansible-operator-controller-manager --follow\n</code></pre>"},{"location":"reference/troubleshooting/#ansiblehost-never-becomes-ready","title":"AnsibleHost never becomes <code>Ready</code>","text":"<p>Likely causes:</p> <ul> <li>Missing or malformed private key. The <code>sshKeySecretRef</code> secret must contain a valid SSH private   key under the key <code>ssh_key</code>. Recreate it with:   <pre><code>kubectl create secret generic &lt;name&gt;-creds \\\n  --from-file=ssh_key=/path/to/private_key --dry-run=client -o yaml | kubectl apply -f -\n</code></pre></li> <li>Host-key scan failed. With <code>ssh.ignoreHostKey: false</code> the operator connects to <code>connection.host</code>   on <code>connection.port</code> to capture the host key. Ensure the address is reachable from inside the   cluster (a <code>*.svc.cluster.local</code> name, a routable IP, etc.), not just from your workstation.</li> <li>Passphrase-protected key. Keys must be usable non-interactively. Provide a key without a   passphrase.</li> </ul>"},{"location":"reference/troubleshooting/#ansiblegroup-shows-healthyfalse-or-referencesvalidfalse","title":"AnsibleGroup shows <code>Healthy=False</code> or <code>ReferencesValid=False</code>","text":"<ul> <li><code>ReferencesValid=False</code> \u2014 one of the referenced hosts or subgroups doesn't exist in the namespace.   Check the names in <code>spec.hosts</code> and <code>spec.groups</code>.</li> <li><code>Healthy=False</code> \u2014 the references exist but at least one member isn't <code>Ready</code>. Fix the underlying   <code>AnsibleHost</code> or subgroup first; group health propagates automatically.</li> </ul>"},{"location":"reference/troubleshooting/#ansiblereconcilejob-is-readyfalse-the-cronjob-is-suspended","title":"AnsibleReconcileJob is <code>Ready=False</code> / the CronJob is suspended","text":"<p>On a reconcile error the controller suspends the generated <code>CronJob</code> and records the reason in the <code>Ready</code> condition. Common causes:</p> <ul> <li>Invalid <code>schedule</code>. The cron expression is rejected when the <code>CronJob</code> is created. Verify the   expression.</li> <li><code>playbookRef</code> points at a missing playbook. Confirm the <code>AnsiblePlaybook</code>   exists in the same namespace.</li> </ul> <p>After fixing the cause, the next reconcile un-suspends the CronJob.</p>"},{"location":"reference/troubleshooting/#a-run-fails-successfulfalse","title":"A run fails (<code>Successful=False</code>)","text":"<p>Look at the pod logs for the most recent job:</p> <pre><code>kubectl get jobs   # find the job created by the cronjob\nkubectl logs job/&lt;job-name&gt; --all-containers\n</code></pre> <p>There are two containers to inspect:</p> <ul> <li>The init container prepares the playbook (cloning from Git for Git-sourced playbooks and   installing requirements). Failures here usually mean an unreachable Git repo, a bad <code>ref</code>, or a wrong   <code>playbookPath</code> / <code>requirementsPath</code>.</li> <li>The runner container executes <code>ansible-playbook</code>. Failures here are normal Ansible errors:   unreachable hosts, host-key mismatches, failed tasks, or privilege-escalation problems.</li> </ul>"},{"location":"reference/troubleshooting/#ssh-host-key-verification-errors-at-run-time","title":"SSH host-key verification errors at run time","text":"<p>The runner uses the aggregated <code>known_hosts</code> built from each host's trusted key. If a host was re-provisioned its key changes and verification fails. Either:</p> <ul> <li>Clear the stored key so the operator re-pins it on the next reconcile (delete/empty the   <code>sshHostKeySecretRef</code> secret), or</li> <li>Set <code>ssh.ignoreHostKey: true</code> on the host if you accept the reduced security.</li> </ul>"},{"location":"reference/troubleshooting/#manual-run-for-debugging","title":"Manual run for debugging","text":"<p>You don't have to wait for the schedule:</p> <pre><code>kubectl create job --from=cronjob/&lt;reconcile-job-name&gt; debug-run\nkubectl logs job/debug-run --all-containers --follow\n</code></pre>"},{"location":"reference/troubleshooting/#still-stuck","title":"Still stuck?","text":"<p>Open an issue at github.com/LightJack05/ansible-operator with the resource YAML, the relevant status conditions, and the failing pod logs.</p>"},{"location":"resources/","title":"Custom Resources","text":"<p>ansible-operator defines four namespaced Custom Resources, all in the API group <code>ansible-operator.lightjack.de</code>, version <code>v1alpha1</code>.</p> Kind Short description <code>AnsibleHost</code> A single SSH-reachable host, its connection details, and credentials. <code>AnsibleGroup</code> A named group of hosts and/or subgroups, mirroring Ansible inventory groups. <code>AnsiblePlaybook</code> A playbook stored inline or sourced from a Git repository. <code>AnsibleReconcileJob</code> A cron schedule that executes a playbook against the generated inventory."},{"location":"resources/#how-they-fit-together","title":"How they fit together","text":"<pre><code>AnsibleReconcileJob \u2500\u2500 playbookRef \u2500\u2500\u25b6 AnsiblePlaybook\n        \u2502\n        \u2502 builds inventory from every host &amp; group in the namespace\n        \u25bc\nAnsibleGroup \u2500\u2500 hosts / groups \u2500\u2500\u25b6 AnsibleHost \u2500\u2500 sshKeySecretRef \u2500\u2500\u25b6 Secret\n</code></pre> <ul> <li>An <code>AnsibleReconcileJob</code> references exactly one <code>AnsiblePlaybook</code>.</li> <li>The inventory for a run is assembled from all <code>AnsibleHost</code> and <code>AnsibleGroup</code> resources in the   same namespace \u2014 not by an explicit reference from the job.</li> <li><code>AnsibleGroup</code>s reference hosts and other groups by name (<code>LocalObjectReference</code>), all within the   same namespace.</li> <li><code>AnsibleHost</code>s reference two Kubernetes <code>Secret</code>s: one for the SSH private key, one for the   trusted host key.</li> </ul>"},{"location":"resources/#status-conditions","title":"Status conditions","text":"<p>Every resource exposes standard Kubernetes conditions under <code>.status.conditions</code>. Inspect them with:</p> <pre><code>kubectl get &lt;kind&gt; &lt;name&gt; -o jsonpath='{.status.conditions}' | jq\n</code></pre> <p>Each condition has a <code>type</code>, a <code>status</code> of <code>True</code> / <code>False</code> / <code>Unknown</code>, plus a <code>reason</code> and <code>message</code>. The condition types per kind are documented on each resource's page.</p>"},{"location":"resources/ansiblegroup/","title":"AnsibleGroup","text":"<p>Represents a group in the Ansible inventory that may contain hosts, subgroups, or both \u2014 mirroring Ansible's inventory group structure. The operator validates that referenced members exist and are healthy, and rolls their health up into the group's status.</p> <ul> <li>API group / version: <code>ansible-operator.lightjack.de/v1alpha1</code></li> <li>Kind: <code>AnsibleGroup</code></li> <li>Scope: Namespaced</li> </ul>"},{"location":"resources/ansiblegroup/#example","title":"Example","text":"<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleGroup\nmetadata:\n  name: foo\n  namespace: default\nspec:\n  ansibleName: foo\n  hosts:\n    - name: baz          # an AnsibleHost in this namespace\n  groups:\n    - name: bar          # another AnsibleGroup in this namespace (a subgroup)\n  groupVars: |\n    ntp_server: pool.ntp.org\n</code></pre>"},{"location":"resources/ansiblegroup/#spec","title":"Spec","text":"Field Type Required Default Description <code>ansibleName</code> string \u2705 \u2014 Name of the group within the Ansible inventory. Must match <code>^[a-zA-Z0-9_.-]+$</code>, max 253 chars. <code>hosts</code> []LocalObjectReference \u2705 \u2014 References to <code>AnsibleHost</code> objects that are members of this group. <code>groups</code> []LocalObjectReference \u2705 \u2014 References to other <code>AnsibleGroup</code> objects that are subgroups of this group. <code>groupVars</code> string \u274c \u2014 Arbitrary string inserted verbatim into this group's Ansible <code>group_vars</code>. <p>Both <code>hosts</code> and <code>groups</code> are required fields, but either may be an empty list (<code>[]</code>). Each entry is a <code>LocalObjectReference</code> \u2014 <code>{ name: &lt;object-name&gt; }</code> \u2014 pointing at an object in the same namespace.</p> <p>Empty lists are valid</p> <p>A leaf group with only hosts still needs <code>groups: []</code>, and a group of subgroups still needs <code>hosts: []</code>.</p>"},{"location":"resources/ansiblegroup/#status","title":"Status","text":"Field Type Description <code>conditions</code> []Condition Standard Kubernetes conditions reflecting the group's state. <p>Condition types:</p> Type Meaning <code>ReferencesValid</code> <code>True</code> when every referenced host and subgroup exists. <code>Healthy</code> <code>True</code> when all referenced hosts and subgroups are themselves healthy. <code>Ready</code> <code>True</code> only when references are valid and all members are healthy. <p>Because the controller indexes host and subgroup references, changing a referenced <code>AnsibleHost</code> or <code>AnsibleGroup</code> automatically re-reconciles the groups that depend on it \u2014 health propagates up the tree.</p>"},{"location":"resources/ansiblegroup/#related","title":"Related","text":"<ul> <li>Members are defined by <code>AnsibleHost</code> resources.</li> <li>Groups (and their nesting) shape the inventory produced by   <code>AnsibleReconcileJob</code>, letting playbooks target <code>hosts: &lt;groupName&gt;</code>.</li> </ul>"},{"location":"resources/ansiblehost/","title":"AnsibleHost","text":"<p>Represents a single host reachable over SSH that Ansible playbooks can target. On reconcile, the operator validates the host's SSH credentials and \u2014 unless disabled \u2014 captures and pins the host's SSH host key.</p> <ul> <li>API group / version: <code>ansible-operator.lightjack.de/v1alpha1</code></li> <li>Kind: <code>AnsibleHost</code></li> <li>Scope: Namespaced</li> </ul>"},{"location":"resources/ansiblehost/#example","title":"Example","text":"<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleHost\nmetadata:\n  name: foobar\n  namespace: default\nspec:\n  ansibleName: foobar\n  connection:\n    host: ssh-server-1.default.svc.cluster.local\n    port: 22\n    user: root\n  ssh:\n    ignoreHostKey: false\n    sshKeySecretRef:\n      name: foobar-creds\n    sshHostKeySecretRef:\n      name: foobar-host-key\n  privilege:\n    become: false\n  hostVars: |\n    http_port: 8080\n    max_clients: 200\n</code></pre>"},{"location":"resources/ansiblehost/#spec","title":"Spec","text":"Field Type Required Default Description <code>ansibleName</code> string \u2705 \u2014 Name used for this host in the generated Ansible inventory. Must match <code>^[a-zA-Z0-9_.-]+$</code>, max 253 chars. <code>connection</code> object \u2705 \u2014 SSH connection parameters. See below. <code>ssh</code> object \u2705 \u2014 SSH authentication configuration. See below. <code>privilege</code> object \u274c \u2014 Privilege escalation settings. See below. <code>hostVars</code> string \u274c \u2014 Arbitrary string inserted verbatim into the host's Ansible <code>host_vars</code>."},{"location":"resources/ansiblehost/#connection","title":"<code>connection</code>","text":"Field Type Required Default Description <code>host</code> string \u2705 \u2014 Hostname or IP to connect to. Must match <code>^[a-zA-Z0-9][a-zA-Z0-9._-]*$</code>, max 253 chars. <code>port</code> integer \u274c <code>22</code> SSH port. Range 1\u201365535. <code>user</code> string \u274c <code>root</code> SSH login user. Must match <code>^[a-z_][a-z0-9_-]*$</code>, max 32 chars. <p>No password authentication</p> <p>There is intentionally no <code>ansible_password</code> field \u2014 the operator only supports key-based SSH auth.</p>"},{"location":"resources/ansiblehost/#ssh","title":"<code>ssh</code>","text":"Field Type Required Default Description <code>ignoreHostKey</code> boolean \u274c <code>false</code> Disable SSH host-key verification. When <code>false</code>, the operator trusts the host key on first connect and stores it in <code>sshHostKeySecretRef</code>. <code>sshKeySecretRef</code> LocalObjectReference \u2705 \u2014 Reference to a <code>Secret</code> containing the SSH private key under the key <code>ssh_key</code>. <code>sshHostKeySecretRef</code> LocalObjectReference \u2705 \u2014 Reference to a <code>Secret</code> used as the trusted host-key store. Populated by the operator when <code>ignoreHostKey</code> is <code>false</code>. <p>A <code>LocalObjectReference</code> is simply <code>{ name: &lt;secret-name&gt; }</code> and always refers to an object in the same namespace.</p>"},{"location":"resources/ansiblehost/#privilege","title":"<code>privilege</code>","text":"Field Type Required Default Description <code>become</code> boolean \u274c <code>false</code> Enable privilege escalation (<code>sudo</code>) for tasks on this host."},{"location":"resources/ansiblehost/#status","title":"Status","text":"Field Type Description <code>conditions</code> []Condition Standard Kubernetes conditions reflecting the host's state. <p>The <code>Ready</code> condition becomes <code>True</code> once the credential secret is valid and the host key has been resolved. It reports <code>False</code> with a reason/message when, for example, the key secret is missing or the host key scan fails.</p>"},{"location":"resources/ansiblehost/#the-credential-secrets","title":"The credential secrets","text":"<p>Create the private-key secret with the private key under <code>ssh_key</code>:</p> <pre><code>kubectl create secret generic foobar-creds \\\n  --from-file=ssh_key=$HOME/.ssh/id_ed25519\n</code></pre> <p>You do not need to create the host-key store secret yourself. The operator creates the secret named in <code>sshHostKeySecretRef</code> on first reconcile and populates it with the trusted host key.</p> <p>Pinning the host key yourself</p> <p>You can optionally pre-create <code>sshHostKeySecretRef</code> with a known host key. When a key is already present, the operator leaves it untouched rather than overwriting it.</p>"},{"location":"resources/ansiblehost/#related","title":"Related","text":"<ul> <li>Reference a host from an <code>AnsibleGroup</code> to organize your inventory.</li> <li>Hosts are automatically included in the inventory built by every   <code>AnsibleReconcileJob</code> in the same namespace.</li> </ul>"},{"location":"resources/ansibleplaybook/","title":"AnsiblePlaybook","text":"<p>Stores an Ansible playbook (and optional <code>requirements.yml</code>) either inline in the resource or by reference to a Git repository. A playbook is consumed at run time by an <code>AnsibleReconcileJob</code>.</p> <ul> <li>API group / version: <code>ansible-operator.lightjack.de/v1alpha1</code></li> <li>Kind: <code>AnsiblePlaybook</code></li> <li>Scope: Namespaced</li> </ul> <p><code>inline</code> and <code>git</code> are mutually exclusive</p> <p>Exactly one of <code>inline</code> or <code>git</code> must be set. This is enforced by a CEL validation rule on the API server (<code>has(self.inline) != has(self.git)</code>), so an invalid spec is rejected at apply time.</p>"},{"location":"resources/ansibleplaybook/#inline-example","title":"Inline example","text":"<p>Inline playbooks are stored as a <code>ConfigMap</code> and mounted into the runner pod.</p> <pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsiblePlaybook\nmetadata:\n  name: inline-playbook\n  namespace: default\nspec:\n  inline:\n    requirements: |\n      - name: ansible.builtin\n        version: 1.0.0\n    playbook: |\n      - name: Example Playbook\n        hosts: all\n        tasks:\n          - name: Ping\n            ansible.builtin.ping:\n</code></pre>"},{"location":"resources/ansibleplaybook/#git-example","title":"Git example","text":"<p>Git playbooks are cloned by an init container in the reconcile job's pod before execution.</p> <pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsiblePlaybook\nmetadata:\n  name: http-git-playbook\n  namespace: default\nspec:\n  git:\n    repo:\n      url: https://example.com/repo.git\n      ref: main\n    playbookPath: path/to/playbook.yaml\n    requirementsPath: path/to/requirements.yml\n</code></pre>"},{"location":"resources/ansibleplaybook/#spec","title":"Spec","text":"Field Type Required Description <code>inline</code> object one of <code>inline</code>/<code>git</code> Playbook (and optional requirements) provided inline. <code>git</code> object one of <code>inline</code>/<code>git</code> Playbook fetched from a Git repository."},{"location":"resources/ansibleplaybook/#inline","title":"<code>inline</code>","text":"Field Type Required Description <code>playbook</code> string \u2705 Contents of the Ansible playbook file. <code>requirements</code> string \u274c Contents of an Ansible <code>requirements.yml</code> file."},{"location":"resources/ansibleplaybook/#git","title":"<code>git</code>","text":"Field Type Required Default Description <code>repo.url</code> string \u2705 \u2014 Git repository URL. <code>repo.ref</code> string \u274c <code>main</code> Git reference (branch, tag, or commit) to check out. <code>playbookPath</code> string \u2705 \u2014 Path to the playbook file within the repository. <code>requirementsPath</code> string \u274c \u2014 Path to a <code>requirements.yml</code> within the repository."},{"location":"resources/ansibleplaybook/#status","title":"Status","text":"Field Type Description <code>conditions</code> []Condition Standard Kubernetes conditions reflecting the playbook's state."},{"location":"resources/ansibleplaybook/#related","title":"Related","text":"<ul> <li>Reference this playbook from an <code>AnsibleReconcileJob</code> via <code>spec.playbookRef</code>   to schedule it.</li> <li>The playbook targets hosts and groups defined by <code>AnsibleHost</code> and   <code>AnsibleGroup</code> resources, via the inventory the reconcile job generates.</li> </ul>"},{"location":"resources/ansiblereconcilejob/","title":"AnsibleReconcileJob","text":"<p>Schedules the execution of an <code>AnsiblePlaybook</code> on a cron schedule. This is the resource that actually makes things happen: the operator generates an Ansible inventory from the hosts and groups in the namespace and manages a Kubernetes <code>CronJob</code> that runs the playbook.</p> <ul> <li>API group / version: <code>ansible-operator.lightjack.de/v1alpha1</code></li> <li>Kind: <code>AnsibleReconcileJob</code></li> <li>Scope: Namespaced</li> </ul>"},{"location":"resources/ansiblereconcilejob/#example","title":"Example","text":"<pre><code>apiVersion: ansible-operator.lightjack.de/v1alpha1\nkind: AnsibleReconcileJob\nmetadata:\n  name: example-reconcile-job\n  namespace: default\nspec:\n  schedule: \"0 0 * * *\"\n  playbookRef:\n    name: some-playbook\n</code></pre>"},{"location":"resources/ansiblereconcilejob/#spec","title":"Spec","text":"Field Type Required Description <code>schedule</code> string \u2705 A cron expression defining when the job runs (e.g. <code>\"0 0 * * *\"</code> for daily at midnight). <code>playbookRef</code> LocalObjectReference \u2705 Reference to the <code>AnsiblePlaybook</code> to execute, in the same namespace. <p>Inventory is built from the namespace, not from <code>playbookRef</code></p> <p>The set of hosts a run targets comes from all <code>AnsibleHost</code> and <code>AnsibleGroup</code> resources in the same namespace. Which of those the playbook acts on is decided inside the playbook itself, via its <code>hosts:</code> selector (e.g. <code>hosts: all</code> or <code>hosts: webservers</code>).</p> <p>Schedule validation</p> <p>The cron <code>schedule</code> is validated by Kubernetes when the underlying <code>CronJob</code> is created. An invalid expression surfaces as a reconcile error and the job's <code>Ready</code> condition goes <code>False</code>.</p>"},{"location":"resources/ansiblereconcilejob/#what-the-operator-creates","title":"What the operator creates","text":"<p>For a reconcile job named <code>&lt;name&gt;</code>, the controller manages several owned objects:</p> Object Name Purpose <code>ConfigMap</code> <code>&lt;name&gt;-inventory</code> The rendered Ansible inventory (<code>hosts.yaml</code>). <code>ConfigMap</code> <code>&lt;name&gt;-known-hosts</code> Aggregated trusted SSH host keys. <code>ConfigMap</code> <code>&lt;name&gt;-job-config</code> Inline playbook data or Git repo details. <code>CronJob</code> <code>&lt;name&gt;</code> Runs the playbook on the schedule. <p>These are regenerated whenever a relevant <code>AnsibleHost</code>, <code>AnsibleGroup</code>, or the referenced <code>AnsiblePlaybook</code> changes.</p>"},{"location":"resources/ansiblereconcilejob/#status","title":"Status","text":"Field Type Description <code>conditions</code> []Condition Standard Kubernetes conditions reflecting the job's state. <p>Condition types:</p> Type Meaning <code>Ready</code> <code>True</code> when the job reconciled successfully and is scheduled. <code>False</code> (with the CronJob suspended) on reconcile errors. <code>Progressing</code> <code>True</code> while a run belonging to this job is executing. <code>Successful</code> <code>True</code> when the last run completed with no failed tasks; <code>False</code> on failure; <code>Unknown</code> before the first completed run."},{"location":"resources/ansiblereconcilejob/#running-on-demand","title":"Running on demand","text":"<p>The generated <code>CronJob</code> runs on its schedule, but you can trigger a run immediately:</p> <pre><code>kubectl create job --from=cronjob/&lt;name&gt; &lt;name&gt;-manual\nkubectl logs job/&lt;name&gt;-manual --all-containers --follow\n</code></pre>"},{"location":"resources/ansiblereconcilejob/#related","title":"Related","text":"<ul> <li><code>AnsiblePlaybook</code> \u2014 what gets executed.</li> <li><code>AnsibleHost</code> / <code>AnsibleGroup</code> \u2014 what builds the inventory.</li> <li>Architecture \u2014 how the CronJob pod is assembled.</li> </ul>"}]}