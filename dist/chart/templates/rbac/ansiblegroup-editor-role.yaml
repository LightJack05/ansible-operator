{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "ansible-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "ansible-operator.resourceName" (dict "suffix" "ansiblegroup-editor-role" "context" $) }}
rules:
- apiGroups:
  - ansible-operator.lightjack.de
  resources:
  - ansiblegroups
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ansible-operator.lightjack.de
  resources:
  - ansiblegroups/status
  verbs:
  - get
{{- end }}
