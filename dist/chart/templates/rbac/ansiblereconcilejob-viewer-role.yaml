{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "ansible-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "ansible-operator.resourceName" (dict "suffix" "ansiblereconcilejob-viewer-role" "context" $) }}
rules:
- apiGroups:
  - ansible-operator.lightjack.de
  resources:
  - ansiblereconcilejobs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ansible-operator.lightjack.de
  resources:
  - ansiblereconcilejobs/status
  verbs:
  - get
{{- end }}
